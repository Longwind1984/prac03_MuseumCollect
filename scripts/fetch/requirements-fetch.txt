# Local Verifier (Phase B) crawl dependencies.
# Install:  pip install -r scripts/fetch/requirements-fetch.txt
# Then for JS-rendered pages:  playwright install chromium
#
# All are open-source, free, no account/payment (no T3 red lines).

httpx>=0.27          # HTTP client (APIs + static pages), follows redirects
trafilatura>=1.8     # extract the main article text out of messy museum HTML
pdfplumber>=0.11     # pull text out of academic PDFs (e.g. dpm.org.cn 院刊)
Pillow>=10.0         # resize/hash images to the ADR-003 size caps (≤512 / ≤1024px)
playwright>=1.44     # headless Chromium for JavaScript-rendered museum sites
